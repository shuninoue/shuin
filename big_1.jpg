function Transparency() {
  const problems = [
    {
      num: '01',
      title: 'The $30 Amazon Trap',
      jp: '偽物',
      body: 'Listings promise "Japanese VG-10 Damascus" but ship from Shenzhen warehouses. The steel has no traceable mill. The "Damascus" pattern is often acid-etched onto a single piece of stainless — not forged from 67 layers. The handle is resin stained to look like pakkawood.',
      tag: 'What you actually get',
      tagValue: 'Unverified steel · etched pattern · plastic core',
    },
    {
      num: '02',
      title: 'The $200 Boutique Markup',
      jp: '中抜き',
      body: 'A genuine VG-10 Damascus knife from a Sakai or Seki forge costs the smith roughly $60–$90 to produce. By the time it reaches a US boutique, it has passed through a Japanese wholesaler, an importer, a distributor, and a retailer — each adding 20–40%. You pay $220 for a $75 blade.',
      tag: 'Where your money goes',
      tagValue: '34% steel & craft · 66% middlemen & retail',
    },
    {
      num: '03',
      title: 'The "Made in Japan" Loophole',
      jp: '産地偽装',
      body: 'A knife can be labeled "Made in Japan" if the final sharpening happens in Japan — even when the blade is forged in China and the steel comes from an unspecified mill. Major brands exploit this. We do not.',
      tag: 'Our standard',
      tagValue: 'Steel, forging, and finishing — all in Japan',
    },
  ];

  return (
    <section id="transparency" style={{
      padding: '160px 0 140px',
      background:
        'radial-gradient(ellipse 60% 40% at 50% 0%, rgba(255, 220, 180, 0.06), transparent 60%),' +
        'var(--bg-1)',
      position: 'relative',
      overflow: 'hidden',
    }}>
      {/* Kanji watermark */}
      <div className="kanji-bg" style={{
        position: 'absolute',
        top: '8%',
        left: '-3%',
        fontSize: 'clamp(220px, 30vw, 440px)',
        color: 'rgba(192, 57, 43, 0.04)',
        zIndex: 0,
      }}>真贋</div>

      <div className="wrap" style={{ position: 'relative', zIndex: 1 }}>
        {/* Header */}
        <div style={{ maxWidth: 780, marginBottom: 96 }}>
          <div className="label eyebrow-line" style={{ marginBottom: 28, color: 'var(--accent)' }}>
            <span>The Problem With Japanese Knives</span>
          </div>
          <h2 className="serif" style={{
            fontSize: 'clamp(40px, 5vw, 72px)',
            lineHeight: 1.04,
            marginBottom: 32,
            textWrap: 'balance',
          }}>
            Most "Japanese knives" online<br/>
            are <em>either fake, or overpriced.</em>
          </h2>
          <p style={{
            color: 'var(--ink-dim)',
            fontSize: 17,
            lineHeight: 1.75,
            maxWidth: 620,
          }}>
            We spent six months buying, cutting with, and destructively testing
            thirty-one knives sold as "Japanese Damascus" online. Here is what
            the market actually looks like.
          </p>
        </div>

        {/* Three problems */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 2 }}>
          {problems.map((p, i) => (
            <div key={i} style={{
              display: 'grid',
              gridTemplateColumns: '140px 1fr 320px',
              gap: 48,
              padding: '56px 0',
              borderTop: '1px solid var(--line)',
              alignItems: 'start',
            }} className="prob-row">
              {/* Number + JP */}
              <div>
                <div className="serif" style={{
                  fontSize: 72,
                  color: 'var(--accent)',
                  fontStyle: 'italic',
                  fontWeight: 400,
                  lineHeight: 1,
                  marginBottom: 14,
                }}>
                  {p.num}
                </div>
                <div className="jp" style={{
                  fontSize: 15,
                  letterSpacing: '0.35em',
                  color: 'var(--ink-sub)',
                  writingMode: 'horizontal-tb',
                }}>
                  {p.jp}
                </div>
              </div>

              {/* Title + body */}
              <div>
                <h3 className="serif" style={{
                  fontSize: 30,
                  marginBottom: 20,
                  fontWeight: 500,
                  lineHeight: 1.2,
                }}>
                  {p.title}
                </h3>
                <p style={{
                  color: 'var(--ink-dim)',
                  fontSize: 15.5,
                  lineHeight: 1.8,
                  maxWidth: 560,
                }}>
                  {p.body}
                </p>
              </div>

              {/* Tag card */}
              <div style={{
                borderLeft: '2px solid var(--accent)',
                padding: '14px 20px',
                background: 'rgba(192, 57, 43, 0.04)',
              }}>
                <div style={{
                  fontSize: 9.5,
                  letterSpacing: '0.3em',
                  textTransform: 'uppercase',
                  color: 'var(--accent)',
                  fontWeight: 500,
                  marginBottom: 10,
                }}>
                  {p.tag}
                </div>
                <div style={{
                  fontFamily: "'Cormorant Garamond', serif",
                  fontSize: 17,
                  lineHeight: 1.5,
                  color: 'var(--ink)',
                  fontStyle: 'italic',
                }}>
                  {p.tagValue}
                </div>
              </div>
            </div>
          ))}
          <div style={{ borderTop: '1px solid var(--line)' }} />
        </div>

        {/* Closing statement */}
        <div style={{
          marginTop: 88,
          padding: '48px 56px',
          background: 'var(--bg-2)',
          borderLeft: '3px solid var(--accent)',
          display: 'grid',
          gridTemplateColumns: '1fr auto',
          gap: 48,
          alignItems: 'center',
        }} className="closing-card">
          <div>
            <div style={{
              fontSize: 10,
              letterSpacing: '0.35em',
              textTransform: 'uppercase',
              color: 'var(--accent)',
              marginBottom: 16,
              fontWeight: 500,
            }}>
              Our answer
            </div>
            <p className="serif" style={{
              fontSize: 'clamp(22px, 2.2vw, 30px)',
              lineHeight: 1.4,
              color: 'var(--ink)',
              fontWeight: 400,
            }}>
              Skip both traps. Buy the <em>same steel</em> the boutiques source
              from, directly from the Tokyo evaluator who vets it — at the price
              it costs before the middlemen take their cut.
            </p>
          </div>
          <a href="#buy" className="btn-ghost" style={{ whiteSpace: 'nowrap' }}>
            See the knife <span style={{ fontSize: 14 }}>→</span>
          </a>
        </div>
      </div>

      <style>{`
        @media (max-width: 900px) {
          .prob-row { grid-template-columns: 1fr !important; gap: 24px !important; }
          .closing-card { grid-template-columns: 1fr !important; padding: 36px 28px !important; }
        }
      `}</style>
    </section>
  );
}

Object.assign(window, { Transparency });
